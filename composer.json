{
  "name": "causeries-quart-heure/app",
  "description": "Application professionnelle de gestion des causeries sécurité — création, signature électronique, export PDF et tableau de bord prévention.",
  "type": "project",
  "keywords": ["sécurité", "causeries", "prévention", "btp", "qse", "signature", "pdf", "pwa"],
  "require": {
    "php": ">=8.1",
    "dompdf/dompdf": "^2.0"
  },
  "autoload": {
    "files": [
      "inc/config.php",
      "inc/database.php"
    ],
    "classmap": [
      "inc/"
    ]
  },
  "config": {
    "optimize-autoloader": true,
    "platform": {
      "php": "8.1"
    }
  },
  "license": "MIT",
  "scripts": {
    "start": "php -S localhost:8080",
    "lint": "php -l index.php && php -l inc/config.php && php -l inc/database.php"
  }
}
